<%= form_for(sabor) do |f| %>
  <% if sabor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sabor.errors.count, "error") %> prohibited this sabor from being saved:</h2>

      <ul>
      <% sabor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
