<%= form_for(salsa) do |f| %>
  <% if salsa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salsa.errors.count, "error") %> prohibited this salsa from being saved:</h2>

      <ul>
      <% salsa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.number_field :valor %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
